# Dependencies (regenerated via npm install)
node_modules/

# Build output / cache
dist/
.angular/

# Graphify cache (regenerable, not source)
graphify-out/cache/

# Real secrets — only *.env.example is tracked
*.env
!*.env.example

# OS / editor noise
.DS_Store
Thumbs.db

# Local Claude Code settings (per-machine, not shared project config)
.claude/settings.json

# Not checked in per user request (2026-07-17)
hc_agile/architecture/decisions/CHECKIN_POLICY.md

# Jupyter autosave checkpoints (regenerable, not source)
.ipynb_checkpoints/

# Generated/leftover bulk data (not source) — see hc_agile PB002/US002
hc_bigdata/data/patient_details/synthea/
hc_bigdata/data/employees.csv

# Synthea CSV dataset copy under AI.HealthCare.Patient.API (same bulk data, not source)
hc_apis/az/hc_core_apis/AI.HealthCare.Patient.API/documents/patient_details/synthea/

# dotnet run output redirected to file during local testing, not source
hc_ai_in/mapi/HC.AI.MAPI/run.log
