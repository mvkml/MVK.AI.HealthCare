# QA — hc_fapi

Playwright API test suite for `hc_fapps` (Dev FastAPI Agent's FastAPI / Azure Functions backend).
Sibling to `hc_qa/api/ai_hc_api/`.

**Status:** Planned — scaffold only, no tests written yet. Mirror `ai_hc_api`'s `package.json` /
`playwright.config.ts` / `tsconfig.json` setup here once the first endpoint is ready to test.

## Layout (once populated)
```
hc_fapi/
├── tests/       ← Playwright specs
└── fixtures/    ← shared test data
```
