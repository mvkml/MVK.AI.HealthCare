# QA — mvkhcapp

Playwright end-to-end UI test suite for `hc_ui/mvkhcapp` (Dev Angular Agent's mobile app).
Sibling to `hc_qa/web/aihcweb/`.

**Status:** Planned — scaffold only, no tests written yet.

## Layout (once populated)
```
mvkhcapp/
├── tests/       ← Playwright specs
└── fixtures/    ← shared test data
```
