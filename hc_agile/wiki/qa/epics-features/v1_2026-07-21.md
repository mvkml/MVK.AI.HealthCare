# Epic → Feature → User Story Hierarchy — v1 (2026-07-21)

Snapshot of `hc_agile/product_owner/backlog/EPICS_AND_FEATURES.md` as of this version. See that
file for the live/current version — this is an archived point-in-time copy for the wiki.

```
Epic: mvkhc Healthcare Platform (ADO #34)
│
├── Feature: Authentication & Identity (ADO #35)
│   ├── PB022 — Auth UI (Login/Signup/Home), mock-first — Done
│   ├── PB023 — Real auth backend (HC.AI.Identity.Api) — Done
│   ├── US009 — Authentication: Login, Sign Up, Home — Done
│   └── US017 — QA Test Coverage for Auth UI (Login & Signup) — Done, ADO #36
│       ├── QA-002 — write test cases (ADO #37)
│       ├── QA-003 — execute Signup + demo3 (ADO #38)
│       └── QA-004 — execute Login (ADO #39)
│
└── Feature: QA Governance (cross-cutting)
    └── US022 — QA Test Coverage Inventory & Untracked Suite Backfill — Done
        ├── QA-006 — backend xUnit coverage audit
        ├── QA-007 — track ai_hc_api suite
        └── QA-008 — track hc_ai_identity_api suite

Epic: Admin Management (ADO #44) — separate epic, established by Dev Angular/Architect work
│
└── Feature: Admin Identity (ADO #45)
    ├── US014 — Admin Login Page (ADO #46) — Done (mock)
    ├── US015 — Admin Sign-Up Page (ADO #47) — Done (mock)
    ├── US016 — Set up HC.AI.Admin.API (ADO #48) — To Do
    └── US018 — QA Test Coverage for Admin Login — In Progress (not yet pushed to ADO)
        └── QA-005 — write Admin Login test cases
```

## Notes on this snapshot
- Two separate Epics exist: **mvkhc Healthcare Platform** (this session's work) and **Admin
  Management** (established independently, already mirrored to ADO by other work on this repo).
  Not merged — they represent genuinely different scopes.
- US018 is drafted locally only; not yet created in Azure DevOps.
