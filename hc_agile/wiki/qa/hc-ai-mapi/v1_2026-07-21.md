# HC.AI.MAPI — v1 (2026-07-21)

**Module:** `hc_qa/api/hc_ai_mapi/tests/provide-prompt.spec.ts`
**Tests:** 8 · **Status:** Closed — Passed (8/8)
**Tracking:** [QA-010](../../../scrum/tasks/QA-010_hc_ai_mapi_persona_routing_playwright.md) · [US021](../../../product_owner/user_stories/US021_persona_required_across_prompt_requests.md)
**Source:** `hc_ai_in/mapi/HC.AI.MAPI/` — Dev Semantic Kernel Agent's persona -> model routing change (`DoctorPromptMapper` -> `LLMModelBL` -> `LLMOptionsFactory`, `HCDocExecutor`/`HCPatientExecutor` appsettings sections)

```
hc_qa/api/hc_ai_mapi/
├── tests/provide-prompt.spec.ts
└── fixtures/prompt.ts   ← buildPromptRequest()
```

Runs against the live `HC.AI.MAPI` (`:5150`) and real Ollama (`:11434`) — real ~7B-model
generations, not mocked. `workers: 1`, 60s per-test timeout.

## Coverage by area
| Area | Cases | Result |
|---|---|---|
| Persona -> model routing | 5 (Doctor, Patient, omitted-default, case-insensitive, unrecognized-fallback) | Pass |
| Validation | 2 (missing message, temperature out of range) | Pass |
| Response shape | 1 (latency/token fields present) | Pass |

## Notes
- `PromptResponse.ModelUsed` (set in `DoctorSemanticProcess.cs` from `item.LLMOptions.Model`) is
  what lets routing be asserted from the HTTP response directly, without inspecting Ollama.
- The "unrecognized persona falls through to Doctor executor" case documents a real, currently
  open gap tracked by **US021** (persona should be required/validated, not silently defaulted) —
  this suite passing does not mean US021 is resolved; it's a regression lock so the eventual fix
  has a test to flip.
- `PersonaModelResolutionController` / `BL/Persona/*`, added in the same diff, are the unrelated
  EPIC001/US011-US013 mock mechanism — not wired into `provide-prompt`, out of scope here.
