# 1. Create the venv using Python 3.12 specifically (not your default 3.14, which PySpark doesn't support yet)
py -3.12 -m venv C:\git\health\hc_bigdata\.venv

# 2. Activate it (only affects this terminal session)
C:\git\health\hc_bigdata\.venv\Scripts\Activate.ps1

# 3. Confirm it's 3.12
python --version

# 4. Install PySpark + JupyterLab into the venv
pip install pyspark==3.5.5 jupyterlab

# 5. (optional but recommended) freeze the exact versions so it's reproducible
pip freeze > C:\git\health\hc_bigdata\requirements.txt

# 6. Launch JupyterLab
jupyter lab


C:\git\health\hc_bigdata\.venv\Scripts\Activate.ps1
cd C:\git\health\hc_bigdata\notebooks\synthea\allergies
python 02_sql_connection.py
