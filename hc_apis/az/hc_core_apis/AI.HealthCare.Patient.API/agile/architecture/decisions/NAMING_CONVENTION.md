# Naming Conventions — AI.HealthCare.Patient.API

**Status:** Not yet established. Project is a fresh, unmodified scaffold — no naming conventions have been needed or decided yet.

Owned by the Architect Agent — to be filled in once real modules/layers exist (e.g. following `ai_hr`'s own convention of `Request`/`Item`/`Response`/carrier-model DTOs, if this project ends up structured similarly).
